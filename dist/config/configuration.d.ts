declare const _default: () => {
    port: number;
    database: {
        uri: string;
    };
    keys: {
        privateKey: string;
        publicKey: string;
        secret: string;
        SMTP_HOST: string;
        SMTP_PORT: string;
        SMTP_USERNAME: string;
        SMTP_PASSWORD: string;
    };
};
export default _default;
